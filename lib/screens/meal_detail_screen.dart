import 'package:flutter/material.dart';

class MealDetails extends StatelessWidget {
  static String routeName="/meal-details";
  @override
  Widget build(BuildContext context) {
    
    return Scaffold(
      appBar: AppBar(title:Text('xyz'),centerTitle:true),
    );
  }
}